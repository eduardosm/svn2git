svn-revs:
  - no: 0
    props:
      svn:sync-from-uuid: This-is-not-a-UUID

conv-params: |
  branches = ["trunk"]
  head = "trunk"

failed: true

logs: |
  E svn2git::convert::stage1: invalid UUID in svn:sync-from-uuid property: "This-is-not-a-UUID"
