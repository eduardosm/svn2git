svn-revs:
  - no: 1
  - no: 1

conv-params: |
  branches = ["trunk"]
  head = "trunk"

failed: true

logs: |
  D svn2git::convert::stage1: importing SVN revision 1
  D svn2git::convert::stage1: importing SVN revision 1
  E svn2git::convert::stage1: non monotonic increasing SVN revision numbers
